apiVersion: v1
kind: Service
metadata:
  name: presentazione-app
  labels:
    app: presentazione-app
spec:
  type: ClusterIP
  selector:
    app: presentazione-app
  ports:
    - name: http
      port: 80
      targetPort: 80
